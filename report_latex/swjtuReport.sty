%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%SWJTU课程论文模板
%%作者：Pungjay
%%GitHub：https://github.com/Pungjay/swjtuReport
%%Overleaf：https://www.overleaf.com/
%%最后更改于2024/05/31
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%在"UCAS课程论文模板"基础上修改而来
%%原作者：jweihe
%%GitHub：https://github.com/jweihe/UCAS_Latex_Template
%%Overleaf：https://www.overleaf.com/latex/templates/zhong-guo-ke-xue-yuan-da-xue-ke-cheng-lun-wen-mo-ban/nphpxhhqvnds

\ProvidesPackage{swjtuReport}

% 页边距
\RequirePackage[left=2.50cm, right=2.50cm, top=2.50cm, bottom=2.50cm]{geometry}

% 导入一些必要的宏包，用户可以根据需要自行添加
\RequirePackage{latexsym,amssymb,amsmath,amsbsy,amsopn,amstext,amsthm,amsxtra,color,bm,calc,ifpdf}
\RequirePackage{graphicx}
\RequirePackage{enumerate}
\RequirePackage{fancyhdr}
\RequirePackage{listings}
\RequirePackage{multirow}
\RequirePackage{makeidx}
\RequirePackage{xcolor}
\RequirePackage{fontspec}
\RequirePackage{subfigure}
\RequirePackage{hyperref}
\RequirePackage{pythonhighlight}
\RequirePackage{tcolorbox}
\RequirePackage{fontawesome}
\RequirePackage{caption}
\RequirePackage{array} % required for text wrapping in tables
\RequirePackage{longtable}
\RequirePackage{rotating}
\RequirePackage{makecell}
\RequirePackage{subcaption}
\RequirePackage{float}

% 去掉图表标题中的冒号，将分隔变为空格
\captionsetup[figure]{labelsep=space}
\captionsetup[table]{labelsep=space}
% \captionsetup{labelformat=default,labelsep=space} % 去除冒号

\numberwithin{equation}{section} % 公式按章节编号
\numberwithin{figure}{section}   % 图按章节编号
\numberwithin{table}{section}    % 表按章节编号

% 更改摘要的标题的字体大小、清除\abstract环境的自动缩进
\RequirePackage{abstract}
\renewcommand{\abstractname}{\textbf{\large {摘\quad 要}}}
\setlength{\absleftindent}{0pt}
\setlength{\absrightindent}{0pt}

% 可固定下划线长度
\makeatletter
\newcommand\dlmu[2][4cm]{\hskip1pt\underline{\hb@xt@ #1{\hss#2\hss}}\hskip3pt}
\makeatother

% 更改引文、交叉引用、链接的颜色
\hypersetup{colorlinks=true,filecolor=black,linkcolor=black,urlcolor=black,citecolor=black}

% 对一些autoref的中文引用名作修改
\def\figureautorefname{图}
\def\tableautorefname{表}
\def\equationautorefname{式}
\def\theoremautorefname{定理}
\def\footnoteautorefname{脚注}
\def\partautorefname{篇}
\def\chapterautorefname{章}
\def\sectionautorefname{节}
\def\subsectionautorefname{小节}
\def\subsubsectionautorefname{小小节}
\def\paragraphautorefname{段落}
\def\subparagraphautorefname{子段落}
\def\FancyVerbLineautorefname{行}
\def\itemautorefname{项}
\def\appendixautorefname{附录}

\newtheorem{Theorem}{定理}[section]
\newtheorem{Lemma}[Theorem]{引理}
\newtheorem{Corollary}[Theorem]{推论}
\newtheorem{Proposition}[Theorem]{命题}
\newtheorem{Definition}[Theorem]{定义}
\newtheorem{Example}[Theorem]{例}

% 参考文献设置
\RequirePackage{natbib}
% \newcommand{\reference}{
%     \bibliographystyle{unsrt}    % 规定了参考文献的格式，建议使用plain,plainnat,abbrvnat内置格式，也可以自制样式.bst文件来导入使用
%     \bibliography{references}    % 调出LaTeX生成参考文献列表
%     %\setcitestyle{open={(},close={)}}
% }
% 自定义参考文献命令，确保显示所有文献
\newcommand{\reference}{%
    \nocite{*} % 强制包含bib文件中的所有文献
    \bibliographystyle{unsrt} % 保持原有风格，如果您有特殊需求，可以更改此行
    \bibliography{references} % 调用参考文献数据库
}

% 文本框设置
\newcommand{\tbox}[1]{
\begin{center}
\begin{tcolorbox}[colback=gray!10,         % gray background
                  colframe=black,          % black frame colour
                  width=8cm,               % Use 8cm total width,
                  arc=1mm, auto outer arc,
                  boxrule=0.5pt,
                 ]
{#1}
\end{tcolorbox}
\end{center}
}

% 页眉设置，若某处留空则不显示
\pagestyle{fancy}
\fancyhead[L]{广东工业大学}
\fancyhead[C]{\fangsong 小车远程控制系统设计与实现}
\fancyhead[R]{课程报告}

% 页尾设置
% % \RequirePackage{fancyhdr}
% \RequirePackage{lastpage} % 用于获取文档总页数
% % \pagestyle{fancy} % 设置默认页眉页脚样式为 fancy
% % \fancyhf{} % 清空页眉页脚的所有默认设置
% \fancyfoot[C]{\thepage/\pageref{LastPage}} % 设置页脚居中显示“页码/总页数”
% % \renewcommand{\headrulewidth}{0pt} % 取消页眉分割线（如有需要）
% % \renewcommand{\footrulewidth}{0.7pt} % 取消页脚分割线（如有需要）

% 封面页设置
{   
    %标题
    \title{
    \vspace{1cm} 
    \heiti \Huge \textbf{{课{\quad}程{\quad}报{\quad}告}} \par
    \vspace{1cm} 
    \heiti \Large {\underline{}}
    \vspace{1cm} 
        }
    %信息
    \author{
    \vspace{0.5cm}
    \kaishu\Large 课程名称\ \dlmu[9cm]{《微机原理与单片机技术》课程设计} \qquad \\ %课程名称
    \vspace{0.5cm}
    \kaishu\Large 题目名称\ \dlmu[9cm]{小车远程控制系统设计与实现} \qquad \\ %课程名称
    \vspace{0.5cm}
    \kaishu\Large 学生学院\ \dlmu[9cm]{机电工程学院} \qquad \\ %学院
    \vspace{0.5cm}
    \kaishu\Large 专业班级\ \dlmu[9cm]{测控技术与仪器1班} \qquad  \\ %专业
    \vspace{0.5cm}
    \kaishu\Large 学{\qquad}号\ \dlmu[9cm]{3122000234}  \qquad  \\ %学号
    \vspace{0.5cm}
    \kaishu\Large 学生姓名\ \dlmu[9cm]{袁汉阔} \qquad  \\ %姓名
    \vspace{0.5cm}
    % \kaishu\Large 联系方式\ \dlmu[9cm]{17811426448} \qquad  \\ %联系方式
    % \vspace{0.5cm}
    % \kaishu\Large 任课教师\ \dlmu[9cm]{张巧芬} \qquad  \\ %任课教师
    % \vspace{0.5cm}
        }
    %日期    
    \date{\today} 
}

% 封面logo设置，logo图片文件（建议以.pdf矢量图形式）放在figures文件夹里
\newcommand{\cover}{
    \begin{figure}
        \centering
        % 移除了垂直间距，或根据需要调整
        \includegraphics[width=\textwidth, keepaspectratio]{figures/广工报告封面.png}
    \end{figure} 
    \maketitle
}

% 目录设置，为目录中的part、chapter、section补上引导点，注意：book或report类型中才有part和chapter，article没有
\RequirePackage[subfigure]{tocloft}
%\renewcommand{\cftpartleader}{\cftdotfill{\cftdotsep}}
%\renewcommand{\cftchapleader}{\cftdotfill{\cftdotsep}}
\renewcommand{\cftsecleader}{\cftdotfill{\cftdotsep}}

%代码显示设置
\lstset{
    breaklines = true,                                   % 自动将长的代码行换行排版
    extendedchars=false,                                 % 解决代码跨页时，章节标题，页眉等汉字不显示的问题
    columns=fixed,       
    numbers=left,                                        % 在左侧显示行号
    basicstyle=\zihao{-5}\ttfamily,
    numberstyle=\small,
    frame=none,                                          % 不显示背景边框
    % backgroundcolor=\color[RGB]{245,245,244},            % 设定背景颜色
    keywordstyle=\color[RGB]{40,40,255},                 % 设定关键字颜色
    numberstyle=\footnotesize\color{darkgray},           % 设定行号格式
    commentstyle=\it\color[RGB]{0,96,96},                % 设置代码注释的格式
    stringstyle=\rmfamily\slshape\color[RGB]{128,0,0},   % 设置字符串格式
    showstringspaces=false,                              % 不显示字符串中的空格
    % frame=leftline,topline,rightline, bottomline         %分别对应只在左侧，上方，右侧，下方有竖线
    frame=shadowbox,                                     % 设置阴影
    rulesepcolor=\color{red!20!green!20!blue!20},        % 阴影颜色
    basewidth=0.6em,
}

\lstdefinestyle{CPP}{
    language=c++,                                        % 设置语言
    morekeywords={alignas,continute,friend,register,true,alignof,decltype,goto,
    reinterpret_cast,try,asm,defult,if,return,typedef,auto,delete,inline,short,
    typeid,bool,do,int,signed,typename,break,double,long,sizeof,union,case,
    dynamic_cast,mutable,static,unsigned,catch,else,namespace,static_assert,using,
    char,enum,new,static_cast,virtual,char16_t,char32_t,explict,noexcept,struct,
    void,export,nullptr,switch,volatile,class,extern,operator,template,wchar_t,
    const,false,private,this,while,constexpr,float,protected,thread_local,
    const_cast,for,public,throw,std},
    emph={map,set,multimap,multiset,unordered_map,unordered_set,
    unordered_multiset,unordered_multimap,vector,string,list,deque,
    array,stack,forwared_list,iostream,memory,shared_ptr,unique_ptr,
    random,bitset,ostream,istream,cout,cin,endl,move,default_random_engine,
    uniform_int_distribution,iterator,algorithm,functional,bing,numeric,},
    emphstyle=\color{CPPViolet}, 
}

\lstdefinestyle{Java}{
    language=[AspectJ]Java,
    keywordstyle=\color{keywordcolor}\bfseries
}

\lstdefinestyle{Python}{
    language=Python,
}